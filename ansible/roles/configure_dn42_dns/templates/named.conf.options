options {
    directory "/var/cache/bind";
    dnssec-validation auto;
    listen-on-v6 { any; };

    // Skip DNSSEC on DN42
    validate-except { dn42; };

    // Setup as recursive resolver
    recursion yes;
    allow-query { {{ this_cidr }}; };
};
